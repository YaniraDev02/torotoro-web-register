export const areaProtegida = {
  async createAreaProtegida(area: string, perimetro: string, descripcion: string): Promise<any> {
    return await areaProtegida.createAreaProtegida(area, perimetro, descripcion)
  }
}